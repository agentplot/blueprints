# Session: e2e-design-loop

## Charge
- Change: flywheel
- Directory: sessions/2026-08-06-e2e-design-loop/
- Tasks: research: run one intent through the design loop end to end,
  subject `spike-context-cleanup`, following `E2E-design-loop.md` §1–§3
  top to bottom. Every command works as written, or the skill, the profile,
  and the schema instructions move until it does.

## Script
`E2E-design-loop.md` is this session's script and this session's to edit —
the walkthrough of §1–§3 (dispatch, the design loop, writeback) that the
run is measured against. It moved here from the repo root, where a
one-file `flywheel/` directory was carrying it; the construction half went
to `sessions/2026-08-06-e2e-construction/` at the same time. The script is
written to the destination, so it names **dispatch** and the
`flywheel-dispatch` profile — the profile rename is a Writeback task and
lands before this run starts, or §1's first command fails.

## Produced
- (pending — the run has not happened)

## Delivered
- (pending)
